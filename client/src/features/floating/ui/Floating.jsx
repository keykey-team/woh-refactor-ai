import { SupportAgentIcon } from "@shared";

const Floating = () => {
  return (
    <button
      type="button"
      className="support-widget"
      aria-label="Підтримка"
    >
      <SupportAgentIcon />
    </button>
  );
};

export default Floating;
